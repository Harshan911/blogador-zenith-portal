// This file is now a placeholder since we're using Netlify CMS
// In a Git-based CMS approach, we don't need a database connection

// The following line is kept for compatibility with existing code
// but it's not actually used anymore
export default {};
